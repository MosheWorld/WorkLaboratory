export const MODEL_BOUNDARIES_HTML = `<p>This is a protocol teaching simulation, not a packet capture, network emulator or cryptographic implementation. It sends no real packets. All addresses use private or documentation ranges, the domain is reserved, and keys, sizes, ports, geography and timing are illustrative. The US West relay is generic infrastructure placed to make both path legs visible.</p><p><b>TCP profile:</b> two client-initiated TCP connections with independent TLS 1.3 sessions and inner application end-to-end encryption. Each animation unit is a modeled TCP segment carrying representative TLS bytes; TLS record boundaries do not generally align with TCP segment boundaries. Modeled IP lengths use 20-byte IPv4 and 20-byte TCP headers. TCP options, MTU discovery, fragmentation, congestion-window evolution, SACK, retransmission backoff, certificate revocation failures, TLS resumption, 0-RTT, ECH and close are omitted.</p><p><b>UDP profile:</b> representative DTLS-SRTP and SRTP bytes are carried inside TURN ChannelData on two client-to-relay allocations. The displayed modeled IP length includes a 20-byte IPv4 header, 8-byte UDP header, 4-byte ChannelData header and the stated inner content. Authenticated signaling, fingerprint exchange, TURN allocation, permissions, channel binding and recipient NAT setup are assumed complete. DTLS flights are grouped and use simplified retry timing. TCP ACKs are drawn separately from TLS segments; real stacks may combine acknowledgments with data. The TLS server flights are paced for teaching and their exact send times do not reproduce an implementation. ICE candidate checks, STUN and TURN setup messages, padding to a four-byte ChannelData boundary over stream transports, and media playback algorithms are not animated.</p><p>ARP and DNS retries are simplified request and response recovery. The shown DNS path is one illustrative gateway-proxied UDP/53 choice; encrypted DNS is common. Inspectable TTL counts only the displayed forwarding nodes, and backbones represent many real hops.</p><p>Protocol definitions: <a href="https://www.rfc-editor.org/rfc/rfc9293.html" target="_blank" rel="noreferrer">TCP (RFC 9293)</a> · <a href="https://www.rfc-editor.org/rfc/rfc768.html" target="_blank" rel="noreferrer">UDP (RFC 768)</a> · <a href="https://www.rfc-editor.org/rfc/rfc8446.html" target="_blank" rel="noreferrer">TLS 1.3 (RFC 8446)</a> · <a href="https://www.rfc-editor.org/rfc/rfc9147.html" target="_blank" rel="noreferrer">DTLS 1.3 (RFC 9147)</a> · <a href="https://www.rfc-editor.org/info/rfc5764/" target="_blank" rel="noreferrer">DTLS-SRTP (RFC 5764)</a> · <a href="https://www.rfc-editor.org/rfc/rfc8656.html" target="_blank" rel="noreferrer">TURN (RFC 8656)</a> · <a href="https://www.rfc-editor.org/rfc/rfc4787.html" target="_blank" rel="noreferrer">NAT behavior (RFC 4787)</a> · <a href="https://www.rfc-editor.org/rfc/rfc9000.html" target="_blank" rel="noreferrer">QUIC (RFC 9000)</a></p>`;
